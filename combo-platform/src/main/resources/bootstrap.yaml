server:
  port: 9200

logging:
  level:
    org.jiang.combo.platform.mapper: debug

spring:
  application:
    name: platform-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://39.104.48.239:3306/base_test
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Root1234@
mybatis:
  mapper-locations: classpath:com/example/platform/dao/*.xml
  type-aliases-package: com.example.platform.dao
  configuration:
    map-underscore-to-camel-case: true
